[pytest]
# 从 backend/ 目录运行；pythonpath=. 让测试能 `import app.*`
# （不依赖 pip install -e .，项目没有打包成可安装包）
pythonpath = .
testpaths = tests

addopts = -q --strict-markers --tb=short

# 重复运行同一用例时按固定顺序，避免共享状态导致偶发失败
filterwarnings =
    ignore::DeprecationWarning
