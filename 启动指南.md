# RAG 企业级知识库问答系统 - 启动指南

## 📋 系统概述

这是一个基于 LangChain 框架开发的 RAG（检索增强生成）企业级知识库问答系统，面向电商平台商品知识库场景。

## 🚀 快速启动

### 前置条件

1. **Python 3.11+** 已安装
2. **MySQL 8.0** 已运行，root 密码为 `root`
3. **Node.js 18+** 已安装
4. **Ollama** 已安装并拉取模型 `qwen2.5:7b`
   ```bash
   ollama pull qwen2.5:7b
   ```

### 启动步骤

#### 1. 启动后端

```bash
# 进入后端目录
cd backend

# 激活虚拟环境（如果还没有）
.\venv\Scripts\activate

# 安装依赖（如果还没有）
pip install -r requirements.txt

# 启动后端服务
python -m uvicorn app.main:app --host 0.0.0.0 --port 8000 --reload
```

#### 2. 启动前端（新终端）

```bash
# 进入前端目录
cd frontend

# 安装依赖（如果还没有）
npm install

# 启动开发服务器
npm run dev
```

#### 3. 访问系统

打开浏览器访问：`http://localhost:5173`

### 默认账号

| 角色 | 用户名 | 密码 |
|------|--------|------|
| 管理员 | admin | 123456 |
| 普通用户 | test | test123 |

管理员拥有知识库管理权限（上传/删除文档），普通用户仅可使用问答功能。

## 🔧 配置说明

### 环境变量配置

后端配置通过 `backend/.env` 文件管理，主要配置项：

| 变量 | 默认值 | 说明 |
|------|--------|------|
| MYSQL_PASSWORD | root | MySQL 密码 |
| OLLAMA_MODEL | qwen2.5:7b | Ollama 模型名 |
| EMBEDDING_MODEL | BAAI/bge-small-zh-v1.5 | Embedding 模型 |
| CHUNK_SIZE | 500 | 文本分块大小 |
| RETRIEVAL_TOP_K | 5 | 检索返回数量 |

### Ollama 模型配置

确保已安装并拉取所需的模型：
```bash
# 安装 Ollama（如果还没有）
# https://ollama.com/download

# 拉取模型
ollama pull qwen2.5:7b

# 可选：拉取 Embedding 模型
ollama pull nomic-embed-text
```

### MySQL 数据库配置

1. 确保 MySQL 服务正在运行
2. 创建数据库（如果需要）：
   ```sql
   CREATE DATABASE rag_knowledge_base CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;
   ```

## 📁 项目结构

```
mydo/
├── backend/                    # Python FastAPI 后端
│   ├── app/
│   │   ├── main.py            # 应用入口
│   │   ├── config.py          # 配置管理
│   │   ├── database.py        # MySQL 连接池
│   │   ├── models/            # ORM 模型
│   │   ├── api/               # 路由处理
│   │   ├── services/          # 业务逻辑
│   │   └── utils/             # 缓存等工具
│   ├── chroma_data/           # 向量数据库存储
│   ├── uploads/               # 上传文档存储
│   └── requirements.txt
├── frontend/                   # React + Vite 前端
│   ├── src/
│   │   ├── pages/             # 页面组件
│   │   ├── components/        # 通用组件
│   │   ├── services/           # HTTP 客户端
│   │   ├── store/             # Zustand 状态管理
│   │   └── types/             # TS 类型定义
│   └── package.json
└── README.md
```

## 🛠️ 功能测试

### 1. 用户认证测试

1. 访问 `http://localhost:5173`
2. 点击"注册"创建新用户
3. 使用注册的账号登录
4. 或使用默认管理员账号登录：admin/123456

### 2. 问答功能测试

1. 创建新会话
2. 输入问题，如："有什么商品？"
3. 查看流式回答和知识库引用
4. 测试多轮对话功能

### 3. 知识库管理测试（仅管理员）

1. 登录管理员账号
2. 访问 `/admin` 路径
3. 上传 PDF/TXT/MD/CSV/DOCX 文档
4. 查看文档处理状态
5. 测试重新索引功能

### 4. API 测试

启动后端后访问 `http://localhost:8000/docs` 查看 Swagger UI，可以测试所有 API 接口。

## 🐛 常见问题

### 1. 后端启动失败

```bash
# 检查端口是否被占用
netstat -ano | findstr :8000

# 如果被占用，使用其他端口
python -m uvicorn app.main:app --host 0.0.0.0 --port 8001 --reload
```

### 2. 前端启动失败

```bash
# 清理缓存重新安装
npm cache clean --force
rm -rf node_modules
npm install
```

### 3. Ollama 连接失败

- 确保 Ollama 服务正在运行：`ollama serve`
- 检查配置文件中的 OLLAMA_BASE_URL
- 尝试访问：`http://localhost:11434`

### 4. MySQL 连接失败

- 确保 MySQL 服务正在运行
- 检查数据库密码配置
- 确认数据库已创建

### 5. ChromaDB 启动问题

ChromaDB 是内存数据库，首次启动时会自动创建数据目录。

## 🔍 开发说明

### 添加新功能

1. 后端：在 `app/api/` 添加新路由，在 `app/services/` 添加业务逻辑
2. 前端：在 `src/pages/` 添加新页面，在 `src/components/` 添加新组件

### 修改配置

主要配置文件：
- `backend/app/config.py` - 后端配置
- `backend/.env` - 环境变量
- `frontend/src/services/api.ts` - API 配置

### 依赖更新

```bash
# 后端
pip install -r requirements.txt

# 前端
npm update
```

## 📚 相关文档

- [FastAPI 文档](https://fastapi.tiangolo.com/)
- [React 18 文档](https://react.dev/)
- [LangChain 文档](https://python.langchain.com/)
- [Ollama 文档](https://github.com/ollama/ollama)
- [ChromaDB 文档](https://docs.trychroma.com/)

## 🤝 贡献指南

1. Fork 项目
2. 创建功能分支
3. 提交更改
4. 发起 Pull Request

## 📄 许可证

MIT License